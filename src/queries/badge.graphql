query Badge($id: ID!) {
  badge(id: $id) {
    id
    name
    isOfficial
    isCommunity
    uri
    hookAddress
    imageUrl
    creatorAddress
    createdAt
    holdersCount
    holders {
      id
    }
    managers {
      id
      name
      imageUrl
    }
    minters {
      id
      name
      holders {
        id
      }
    }
    burners {
      id
      name
      holders {
        id
      }
    }
    transferers {
      id
      name
      holders {
        id
      }
    }
  }
}
