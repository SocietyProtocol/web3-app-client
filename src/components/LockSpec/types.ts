export enum TierId {
  BRONZE = "bronze",
  SILVER = "silver",
  GOLD = "gold",
}
export enum LockDuration {
  ONE_YEAR = 1,
  TWO_YEARS = 2,
  THREE_YEARS = 3,
  FOUR_YEARS = 4,
}

export enum LockSpecTab {
  LOCK = "lock",
  CLAIM = "claim",
  HISTORY = "history",
}
